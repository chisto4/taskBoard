import { service as _service, singleton } from 'knifecycle';

function noop() {
  return undefined;
}

export default singleton(_service(initRandom, "random", ["?log"]));

async function initRandom({
  log = noop
}) {
  log('debug', '🎲 - Random service initialized.');
  return random;

  function random() {
    const num = Math.random();
    log('debug', '🎲 - Created a random number:', num);
    return num;
  }
}
//# sourceMappingURL=random.mjs.map