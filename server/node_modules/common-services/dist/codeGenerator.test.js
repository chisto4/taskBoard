"use strict";

var _codeGenerator = _interopRequireDefault(require("./codeGenerator"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('codeGenerator', () => {
  const log = jest.fn();
  const random = jest.fn();
  beforeEach(() => {
    log.mockReset();
    random.mockReset();
  });
  test('should work', async () => {
    random.mockReturnValueOnce(0.5);
    const codeGenerator = await (0, _codeGenerator.default)({
      random,
      log
    });
    const code = await codeGenerator();
    expect({
      code,
      logCalls: log.mock.calls,
      randomCalls: random.mock.calls
    }).toMatchSnapshot();
  });
});
//# sourceMappingURL=codeGenerator.test.js.map