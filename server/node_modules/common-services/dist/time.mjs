import { service as _service, singleton } from 'knifecycle';

function noop() {
  return undefined;
}

export default singleton(_service(initTime, "time", ["?log"]));

async function initTime({
  log = noop
}) {
  log('debug', '⏰ - Time service initialized.');
  return time;

  function time() {
    const now = Date.now();
    log('debug', '⏰ - Picked a timestamp:', now);
    return now;
  }
}
//# sourceMappingURL=time.mjs.map