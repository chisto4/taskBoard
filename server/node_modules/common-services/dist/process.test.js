"use strict";

var _yerror = _interopRequireDefault(require("yerror"));

var _knifecycle = _interopRequireWildcard(require("knifecycle"));

var _process = _interopRequireDefault(require("./process"));

function _getRequireWildcardCache() { if (typeof WeakMap !== "function") return null; var cache = new WeakMap(); _getRequireWildcardCache = function () { return cache; }; return cache; }

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } if (obj === null || typeof obj !== "object" && typeof obj !== "function") { return { default: obj }; } var cache = _getRequireWildcardCache(); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('Process service', () => {
  const log = jest.fn();
  const savedProcessName = global.process.title;
  const processListenerStub = jest.spyOn(global.process, 'on');
  let exit;
  let exitPromise;
  let fatalErrorDeferred;
  beforeEach(() => {
    exitPromise = new Promise(resolve => {
      exit = jest.fn(resolve);
    });
    processListenerStub.mockClear();
    log.mockReset();
  });
  afterEach(() => {
    global.process.title = savedProcessName;
  });
  describe('', () => {
    beforeEach(done => {
      (0, _process.default)({
        NODE_ENV: 'development',
        PROCESS_NAME: 'Kikooolol',
        log,
        exit,
        $instance: {
          destroy: () => Promise.resolve()
        },
        $fatalError: {
          promise: new Promise((resolve, reject) => {
            fatalErrorDeferred = {
              resolve,
              reject
            };
          })
        }
      }).then(() => done()).catch(done);
    });
    test('should work', () => {
      expect(log.mock.calls).toEqual([['warning', '🔂 - Running in "development" environment.'], ['debug', '📇 - Process service initialized.']]);
      expect(global.process.title).toEqual('Kikooolol - development');
      expect(processListenerStub.mock.calls.length).toEqual(3);
    });
    test('should handle fatal errors', done => {
      fatalErrorDeferred.reject(new _yerror.default('E_AOUCH'));
      exitPromise.then(() => {
        expect(exit.mock.calls).toEqual([[1]]);
      }).then(() => done()).catch(done);
    });
    test('should handle uncaught exceptions', done => {
      processListenerStub.mock.calls.find(call => 'uncaughtException' === call[0])[1](new _yerror.default('E_AOUCH'));
      exitPromise.then(() => {
        expect(exit.mock.calls).toEqual([[1]]);
      }).then(() => done()).catch(done);
    });
    ['SIGINT', 'SIGTERM'].forEach(signal => test('should handle `signal`', done => {
      processListenerStub.mock.calls.find(call => signal === call[0])[1](new _yerror.default('E_AOUCH'));
      exitPromise.then(() => {
        expect(exit.mock.calls).toEqual([[0]]);
      }).then(() => done()).catch(done);
    }));
  });
  test('should work with Knifecycle', done => {
    new _knifecycle.default().register(_process.default).register((0, _knifecycle.constant)('log', log)).register((0, _knifecycle.constant)('NODE_ENV', 'production')).register((0, _knifecycle.constant)('exit', exit)).run(['process']).then(() => {
      expect(log.mock.calls).toEqual([['warning', '🔂 - Running in "production" environment.'], ['debug', '📇 - Process service initialized.']]);
    }).then(() => done()).catch(done);
  });
});
//# sourceMappingURL=process.test.js.map