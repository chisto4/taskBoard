import { service as _service, singleton } from 'knifecycle';

function noop() {
  return undefined;
}

const DEFAULT_COUNTER = {
  firstCount: 1
};
export default singleton(_service(initCounter, "counter", ["?COUNTER", "?log"]), true);

async function initCounter({
  COUNTER = DEFAULT_COUNTER,
  log = noop
}) {
  let currentCount = COUNTER.firstCount;
  log('debug', '📇 - Counter service initialized.');
  return counter;

  async function counter() {
    log('debug', '📇 - Picked a count:', currentCount);
    return currentCount++;
  }
}
//# sourceMappingURL=counter.mjs.map